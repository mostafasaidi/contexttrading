"""ContextTrading — deterministic Smart Money Concepts analysis engine.

Python calculates, AI explains. Every module returns structured, versioned
JSON; identical input data yields identical output.
"""

__version__ = "0.1.0"
